server:
  port: 8080              # ??? ??? ?? (?? 8080)
  servlet:
    context-path: /       # ?? URL? ?? ?? (/? ??? ??)
  shutdown: graceful      # ?? ?? ? ?? ?? ??? ? ?? ??

spring:
  application:
    name: vacation-calculator   # ?????? ?? (??/????/Actuator? ??)

  profiles:
    active: local          # ?? ???? Spring Profile (local, prod ? ???)

  thymeleaf:
    cache: false           # ??? ?? ???? (?? ? ???? ?? ??)
    prefix: classpath:/templates/ # HTML ??? ??
    suffix: .html          # ??? ???
    mode: HTML             # Thymeleaf ?? ?? (HTML ??)

  web:
    resources:
      static-locations:
        - classpath:/static/   # ?? ???(JS/CSS/???) ??
      cache:
        period: 0          # ?? ??? ?? ???? (???)


logging:
  level:
    root: INFO             # ?? ?? ?? ??
    com.vacation: DEBUG    # ?? ???? ???? ?? ?? ??
    org.springframework.web: INFO  # ??? ? ?? ??? INFO ??

management:
  endpoints:
    web:
      exposure:
        include: health,info      # Actuator?? ?? ??? ????? (/actuator/health, /info)
  endpoint:
    health:
      show-details: when_authorized # ??? ?????? ?? ?? ?? ??
